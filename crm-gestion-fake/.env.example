# Fake CRM Gestion receiver config.
# These two secrets MUST match what api-gateway sends (CRM_API_KEY / CRM_HMAC_SECRET).
PORT=5600
WEBHOOK_PATH=/webhook/payments
CRM_API_KEY=dev-crm-api-key
CRM_HMAC_SECRET=dev-crm-hmac-secret
