node_modules/
dist/
.env*
# keep example/template env files in git (they contain no real secrets)
!.env.example
!*.env.example
*.js.map
/image-pipeline\images/
# server backups (DB dumps + MinIO archives) — never commit
infra/backups/
infra/backup.log