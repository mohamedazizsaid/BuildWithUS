FROM node:20-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

EXPOSE 3000
# -H 0.0.0.0 makes Next.js listen on all interfaces (required in containers)
CMD ["npx", "next", "dev", "-H", "0.0.0.0", "-p", "3000"]