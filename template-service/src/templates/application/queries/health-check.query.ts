import type { HealthCheckRequest } from 'proto/generated/common';

/**
 * Health Check Query
 *
 * Query to check service health status.
 * Implements the proto interface for type safety.
 */
export class HealthCheckQuery implements HealthCheckRequest {}
